<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudyMate">
        <activity android:name=".StudyHistoryActivity" />
        <activity android:name=".MyPageActivity" />
        <activity android:name=".WrongAnswerActivity" />
        <activity android:name=".QuizResultActivity" />
        <activity android:name=".QuizActivity" />
        <activity android:name=".SummaryResultActivity" />
        <activity android:name=".StudyInputActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".EmailVerificationActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
