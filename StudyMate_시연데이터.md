# StudyMate 발표용 시연 데이터

> 백엔드 서버가 응답하지 않을 경우 아래 데이터를 활용해 시연합니다.

---

## 1. 테스트 입력 텍스트 (학습 내용 입력란에 붙여넣기)

```
머신러닝(Machine Learning)은 컴퓨터가 명시적으로 프로그래밍되지 않고도 데이터로부터 스스로 학습하는 인공지능의 한 분야이다.

학습 방식에 따라 세 가지로 분류된다. 첫째, 지도학습(Supervised Learning)은 입력과 정답 레이블이 쌍으로 주어진 데이터셋을 이용해 모델을 훈련시키는 방법으로, 분류(Classification)와 회귀(Regression) 문제에 사용된다. 둘째, 비지도학습(Unsupervised Learning)은 레이블 없이 데이터의 패턴이나 구조를 스스로 찾아내며, 군집화(Clustering)와 차원 축소(Dimensionality Reduction)에 활용된다. 셋째, 강화학습(Reinforcement Learning)은 에이전트가 환경과 상호작용하며 보상을 최대화하는 방향으로 정책을 학습한다.

모델 평가 지표로는 정확도(Accuracy), 정밀도(Precision), 재현율(Recall), F1 Score가 있다. 과적합(Overfitting)은 훈련 데이터에는 잘 맞지만 새로운 데이터에 일반화되지 않는 현상이며, 이를 방지하기 위해 드롭아웃(Dropout), 정규화(Regularization), 교차검증(Cross-Validation) 기법을 사용한다.
```

---

## 2. 더미 요약 응답 JSON (`/summary` 엔드포인트 응답 형식)

파서 요구사항: `summary` 3~5개, `keywords` 정확히 3개

```json
{
  "summary": [
    "머신러닝은 데이터로부터 스스로 학습하는 인공지능 기술로, 지도학습·비지도학습·강화학습 세 가지 방식으로 분류된다.",
    "지도학습은 레이블된 데이터로 분류와 회귀 문제를 해결하고, 비지도학습은 레이블 없이 군집화와 차원 축소를 수행한다.",
    "강화학습은 에이전트가 환경과 상호작용하며 보상을 최대화하는 방향으로 정책을 학습한다.",
    "모델 평가 지표로 정확도, 정밀도, 재현율, F1 Score를 활용하며 과적합 방지가 중요하다."
  ],
  "keywords": [
    "지도학습",
    "과적합",
    "강화학습"
  ]
}
```

---

## 3. 더미 퀴즈 응답 JSON (`/quiz` 엔드포인트 응답 형식)

파서 요구사항: 정확히 3개, 각 퀴즈는 `options` 4개, `answerIndex` 0~3, `explanation` 비어있지 않아야 함

```json
[
  {
    "question": "레이블(정답)이 없는 데이터에서 패턴을 스스로 찾아내는 머신러닝 학습 방식은?",
    "options": [
      "비지도학습",
      "지도학습",
      "강화학습",
      "전이학습"
    ],
    "answerIndex": 0,
    "explanation": "비지도학습은 정답 레이블 없이 데이터의 군집이나 구조를 스스로 발견하는 방식입니다. 군집화(K-Means 등)와 차원 축소(PCA 등)가 대표적입니다."
  },
  {
    "question": "훈련 데이터에는 높은 성능을 보이지만 새로운 데이터에 일반화되지 않는 현상을 무엇이라 하는가?",
    "options": [
      "언더피팅",
      "정규화",
      "과적합",
      "드롭아웃"
    ],
    "answerIndex": 2,
    "explanation": "과적합(Overfitting)은 모델이 훈련 데이터의 노이즈까지 학습해 새로운 데이터에 대한 예측 성능이 떨어지는 현상입니다. 드롭아웃, 정규화, 교차검증으로 방지할 수 있습니다."
  },
  {
    "question": "다음 중 모델 성능 평가 지표가 아닌 것은?",
    "options": [
      "정확도(Accuracy)",
      "재현율(Recall)",
      "학습률(Learning Rate)",
      "F1 Score"
    ],
    "answerIndex": 2,
    "explanation": "학습률(Learning Rate)은 모델 훈련 시 파라미터 업데이트 속도를 조절하는 하이퍼파라미터로, 성능 평가 지표가 아닙니다. 평가 지표는 정확도, 정밀도, 재현율, F1 Score 등입니다."
  }
]
```

---

## 4. 시연 시나리오 (발표 흐름)

### 정상 시나리오 (백엔드 서버 연결 시)
1. 앱 실행 → 로그인 화면 (테스트 계정: `test@studymate.com` / `Test1234!`)
2. 홈 화면 → **새 학습 시작** 탭
3. 위 테스트 텍스트 입력 후 **AI 요약 생성** 버튼 클릭
4. 요약 4개 + 키워드 3개 결과 확인
5. **퀴즈 생성** 버튼 클릭 → 4지선다 3문제 풀이
6. 결과 화면 → 오답 확인 → **오답노트** 탭
7. **마이페이지** → 학습 기록·퀴즈 풀이 수·평균 점수 확인

### 백업 시나리오 (서버 불안정 시)
- 위 JSON을 백엔드 서버의 더미 응답으로 하드코딩하거나
- `AiService.java`에서 실제 HTTP 호출 대신 더미 데이터를 직접 반환하도록 임시 수정

```java
// AiService.java - 발표용 임시 더미 응답 (백엔드 없을 때)
// summarize() 메서드 상단에 추가:
String dummySummary = "{\"summary\":[\"머신러닝은 데이터로부터 스스로 학습하는 인공지능 기술로, 지도학습·비지도학습·강화학습 세 가지 방식으로 분류된다.\",\"지도학습은 레이블된 데이터로 분류와 회귀 문제를 해결하고, 비지도학습은 레이블 없이 군집화와 차원 축소를 수행한다.\",\"강화학습은 에이전트가 환경과 상호작용하며 보상을 최대화하는 방향으로 정책을 학습한다.\",\"모델 평가 지표로 정확도, 정밀도, 재현율, F1 Score를 활용하며 과적합 방지가 중요하다.\"],\"keywords\":[\"지도학습\",\"과적합\",\"강화학습\"]}";
callback.onSuccess(AiResponseParser.parseSummary(dummySummary));
return;
```

---

## 5. 테스트 계정 정보

| 항목 | 값 |
|------|-----|
| 이메일 | test@studymate.com |
| 비밀번호 | Test1234! |
| 비고 | Firebase Console에서 미리 생성 필요 |

> Firebase Console → Authentication → Add user 에서 위 계정을 미리 등록해두면 발표 당일 로그인 과정도 매끄럽게 시연 가능합니다.
