{
  "name": "studymate-firestore-rules-tests",
  "private": true,
  "scripts": {
    "test": "node --test firestore.rules.test.js",
    "test:emulator": "firebase emulators:exec --config ../../firebase.json --project studymate-rules-test --only firestore \"npm test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0",
    "firebase-tools": "^14.0.0"
  }
}
